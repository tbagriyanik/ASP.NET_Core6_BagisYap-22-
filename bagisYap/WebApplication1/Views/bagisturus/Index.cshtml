@model IEnumerable<WebApplication1.Models.bagisturu>

@{
    ViewData["Title"] = "Index";
}

<h1>Bağış Türleri - Yönetim</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.bagisAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.bagisDetay)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.resim)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.bagisAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.bagisDetay)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.resim)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
